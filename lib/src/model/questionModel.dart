class Question {
  String questionImage;
  String questionText;
  bool questionAnswer;

  Question({String i, String q, bool a}) {
    questionImage = i;
    questionText = q;
    questionAnswer = a;
  }
}
